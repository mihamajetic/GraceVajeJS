<script lang="ts">
	// Napiši funkcijo, ki iz tabele števil vrne samo tista, ki so deljiva s 3 ali 5.

	function filtrirajDeljiva(s: number[]) {}

	const stevila: number[] = [1, 3, 5, 7, 10, 15, 22, 30];
	console.log(filtrirajDeljiva(stevila));
	// Pričakovan rezultat [3, 5, 10, 15, 30]
</script>

<code>
	<pre
		style="white-space: pre-wrap; word-wrap: break-word; overflow-wrap: break-word">// Napiši funkcijo, ki iz tabele števil vrne samo tista, ki so deljiva s 3 ali 5.</pre>
	<pre
		style="white-space: pre-wrap; word-wrap: break-word; overflow-wrap: break-word">// V mapi /routes ustvari podmapo naloga7/, v njej datoteko +page.svelte in dodaj naslednjo kodo - znotraj script elementa seveda.</pre>
	<br />
	{@html `<pre>
function filtrirajDeljiva(s: number[]) {
    
}

const stevila: number[] = [1, 3, 5, 7, 10, 15, 22, 30];
console.log(filtrirajDeljiva(stevila));
// Pričakovan rezultat [3, 5, 10, 15, 30]

</pre>`}
</code>
<hr />
<p class="text-xl">Za testiranje odpri konzolo.</p>
